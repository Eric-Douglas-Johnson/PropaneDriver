// Intentionally empty. Every setting this app reads now comes from App Service
// application settings in production and local.settings.json locally, so that
// nothing environment-specific is committed and the two can never cross.
// See local.settings.example.json for the full list of keys and which are
// required. The file itself is kept so the default configuration source still
// resolves and so an operator has somewhere obvious to put a genuinely
// environment-independent default.
{
}
